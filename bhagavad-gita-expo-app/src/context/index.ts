// Main app context
export { AppProvider, useAppContext, AppContext } from './AppContext';

// Re-export types for convenience
export type { AppState, AppActions, AppContext as AppContextType } from '../types';
